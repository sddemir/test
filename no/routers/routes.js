const router = require('express').Router(); // express router modülünü dahil eder.
const {} = require('../controller/controller'); // controller dosyasından fonksiyonları dahil eder.

// router.post('/örn', örn); 

module.exports = router; 